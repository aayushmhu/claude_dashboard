# Rule Library Audit — 2026-05-16

**Auditor**: Insights Engineer (dispatched by Team Lead)  
**DB**: `~/.claude-dashboard/dashboard.db`  
**Scope**: All 15 rules in `app/api/insights/route.ts`

All SQL dry-runs use JULIANDAY arithmetic where TIMESTAMPDIFF is unavailable in the sqlite3 CLI. Model column confirmed populated on Stop/SubagentStop rows (762 Opus rows, 0 NULL model). All PostToolUse model values are NULL (16,128 rows) — confirming the logger limitation documented in feedback_analytics_rules.md.

---

## Per-Rule Table

| Rule ID | SQL executes? | Fires on live DB? (count) | Body text reads correctly? | Edge cases verified | Verdict |
|---|---|---|---|---|---|
| `opus-trivial-tools` | Yes | No (turn_count=2, threshold=5) | Yes — body text and rates match code ($25/M Haiku) | Model on Stop/SubagentStop is fine; threshold not reached, not a SQL bug | **OK** |
| `subagent-cache-miss` | Yes | No (cache_ratio=94.8%, threshold <30%) | Yes | Cache ratio formula correct (total_input+cache_read denominator). Rule correctly silent — user has healthy caching | **OK** |
| `edit-retries` | Yes | No (session_count=0) | Yes | No Edit failures in last 30 days — rule correctly silent | **OK** |
| `long-running-tools` | Yes | Yes (57 slow calls; top offender: Agent, 51 calls, peak 45,373s) | Yes | Duration threshold 60s, min_calls=5 both met. Peak 45,373s = ~12.6h Agent call | **OK** |
| `daily-cost-spike` | Yes | Yes (May 14: $111, baseline $36, ratio=3.08×) | Yes | Trailing 7-day window has ≥3 days data; min_baseline=$0.50 met | **OK** |
| `opus-verbose-output` | Yes | No (turns=2, threshold=10) | Spec stale — spec says "$75/M", code correctly uses "$25/M" (current pricing). Route body text correct. | input>500 and output>2000 gates working | **Fix needed (spec doc only — update `opus-verbose-output.md` to say $25/M not $75/M)** |
| `file-read-thrashing` | Yes | Yes (client.tsx: 282 reads across 3 sessions) | Yes | json_extract on tool_input working; per-session threshold met | **OK** |
| `cache-write-without-read` | Yes | No (session_count=2, threshold=3) | Yes | 2 sessions with cache writes but no reads; one below the 3-session threshold | **OK** |
| `tool-error-retry-loops` | Yes | No (1 session qualifies, threshold=2) | Yes | Session eb7fc7cf has 4 consecutive Bash errors; only 1 session total, needs ≥2 | **OK** |
| `prompt-caching-not-enabled` | Yes | No (session_count=0) | Yes | All recent sessions with ≥50k input tokens have caching active | **OK** |
| `subagent-explosion` | Yes | Yes (3 sessions with ≥20 subagent calls) | Yes | avg_calls=78, max_calls=115; exceeds both thresholds (min_sessions=2, min_calls=20) | **OK** |
| `daily-volume-anomaly` | Yes | No (max ratio=1.48×, threshold=3.0×) | Yes | May 14: 264 events vs 178.4 baseline; ratio below threshold | **OK** |
| `high-tool-error-rate` | Yes | No (session_count=0) | Yes | No sessions with ≥10 tool calls AND ≥30% error rate in last 30 days | **OK** |
| `opus-on-research-tasks` | Yes | Yes (session_count=2, opus_cost=$10.59) | Yes | 2 sessions with ≥10 tool calls and zero Edit/Write calls, all on Opus | **OK** |
| `opus-small-output` | Yes | No (turns=0) | Yes | No Opus turns with ≥1000 input tokens and ≤200 output tokens in last 30 days | **OK** |

---

## Key Finding: CEO's hypothesis was partially correct but misattributed

The CEO brief stated "opus-trivial-tools and opus-on-research-tasks return 0 results because they query model on PostToolUse rows where it's NULL."

**This is not accurate.** Both rules query model on `Stop`/`SubagentStop` rows, where model IS populated (762 Opus rows, 0 null). The SQL itself is correct.

The actual explanation for the zero result in the prior dry-run:

- **opus-trivial-tools** (turn_count=2 in current run, was 0 in prior run): The rule fired 0 at the time because there were fewer qualifying sessions in the 30-day window at that moment. Today there are 2 turns — below the threshold of 5. Not a bug.
- **opus-on-research-tasks** (session_count=2 now, was 0 in prior run): The prior dry-run may have been run at a different time when sessions were outside the 30-day window. Today it fires with session_count=2 = threshold (2).

The prior dry-run that showed 0 for both may have been using a different window or the "model on PostToolUse" theory may have been tested incorrectly by querying PostToolUse events directly rather than checking what the rules actually JOIN to.

**The SQL is correct. No rule rewrites are needed.**

---

## Rules that currently fire (on 2026-05-16 live data)

| Rule | Key metric |
|---|---|
| `long-running-tools` | 57 slow calls, peak 12.6h Agent call |
| `daily-cost-spike` | May 14: $111 vs $36 baseline (3.08×) |
| `file-read-thrashing` | client.tsx: 282 reads across 3 sessions |
| `subagent-explosion` | 3 sessions with 78+ subagent calls (peak 115) |
| `opus-on-research-tasks` | 2 sessions, $10.59 Opus cost, no Edit/Write |

5 of 15 rules currently surface cards. The other 10 are correctly silent given the live data patterns (no edit failures, healthy caching, low error rates, no cost/volume anomalies beyond May 14).

---

## Spec doc maintenance issues (non-blocking)

1. **`opus-verbose-output.md`**: References "$75/M" Opus output rate. Code correctly uses $25/M (updated 2026-05-14 per pricing memory). Spec doc is stale. Update the spec doc to match the current rate.
2. **`opus-trivial-tools.md`**: References "$15/$75/$18.75/$1.50 per M" in the Edge cases section. These are pre-2026-05-14 rates. Current rates are $5/$25/$10/$0.50. Update spec for accuracy (or leave as historical note per pricing memory's guidance — "leave doc files alone unless asked").
3. **`subagent-cache-miss.md`**: Validation section shows "$1281/day" cost figures from May 13. Current May 14 peak is $111. The absolute numbers in the validation section are now stale (different day's data) — not a logic issue, just stale examples.

These are spec doc hygiene issues. The route logic is correct for all three.

---

## Proposed actions (for Team Lead review)

1. **No rule SQL changes needed.** All 15 rules execute correctly. The 2 rules that were flagged as "0 results" now fire or are below threshold for valid data reasons.
2. **Spec doc fix**: Update `opus-verbose-output.md` to say $25/M (not $75/M). One-line change, no code impact.
3. **Memory update**: Update `feedback_analytics_rules.md` to note that the "opus-trivial-tools and opus-on-research-tasks broken" finding was a false alarm — the rules query Stop/SubagentStop where model is populated; the prior 0-result was a data window issue, not a SQL bug.

---

*Dry-run completed 2026-05-16. DB: `~/.claude-dashboard/dashboard.db`. 293 SubagentStop events, 762 Opus Stop/SubagentStop events, 16,128 PostToolUse events (all model=NULL, confirming logger limitation but not affecting any of the 15 rules).*

---

## 2026-08-19 Post-pricing-update verdict

**Context**: `pricing-update-002` landed. New 7-branch `COST_SQL` replaces the 3-branch fallback (opus / haiku / else=sonnet).
Changes:
- `%fable%` / `%mythos%` → +3.3× ($3/$15 default → $10/$50) [278 events in DB, 30-day window]
- `%sonnet-5%` → −33% ($3/$15 default → $2/$10) [187 events in DB]
- `%opus-4-1%` → +3× ($5/$25 → $15/$75) [retired, **0 events in DB**]
- `%haiku-3-5%` → slight decrease ($1/$5 → $0.80/$4) [retired, **0 events in DB**]
- opus-4.x / opus-5 / sonnet-4-6 / haiku-4-5: **unchanged**

**Model mix in 30-day window** (Stop/SubagentStop with input_tokens NOT NULL):

| Model | Events | Old cost | New cost | Δ |
|---|---:|---:|---:|---|
| claude-opus-5 | 743 | $3,860.92 | $3,860.92 | 0 |
| claude-fable-5 | 278 | $482.25 | $1,607.49 | **+$1,125.24 (+3.33×)** |
| claude-sonnet-5 | 187 | $1,403.60 | $935.73 | **−$467.87 (−33%)** |
| claude-opus-4-8 | 35 | $256.12 | $256.12 | 0 |
| claude-opus-4-7 | 9 | $8.89 | $8.89 | 0 |
| claude-sonnet-4-6 | 4 | $5.34 | $5.34 | 0 |
| claude-haiku-4-5-20251001 | 2 | $1.19 | $1.19 | 0 |
| (null) | 12 | $0.00 | $0.00 | 0 |
| **Total** | 1,270 | **$6,018.31** | **$6,675.68** | +$657.37 (+10.9%) |

The fable increase dominates; the sonnet-5 decrease partially offsets it.

### Rules re-audited (cost-sensitive)

- **`opus-trivial-tools`** (Rule 1): **PASS — unchanged**. Filter is `model LIKE '%opus%'`; all opus rows in DB are opus-4.x/opus-5 (unchanged $5/$25). Dry-run: `turn_count=1, actual_cost=$0.88 (old)=$0.88 (new), haiku_cost=$0.18`. Below `opus_min_turns=5` threshold under both formulas → correctly silent. Body text ("Haiku handles lookup-style work at a fraction of the cost") still accurate.

- **`daily-cost-spike`** (Rule 5): **CHANGED — different day surfaces**. Rule fires under both formulas but reports a different peak:
  - **OLD**: spike on 2026-08-13, cost $1,105.44, baseline $312.59, ratio 3.54×
  - **NEW**: spike on 2026-08-10, cost $991.11, baseline $255.74, ratio 3.88×
  - Cause: 2026-08-10 is dominated by fable-5 activity → cost triples under new formula while its trailing-7d baseline (which mixes fable / sonnet-5 / opus days) only rises modestly. 08-13 was already flagged under old pricing at exactly its threshold; under new pricing its own baseline rises (past 7 days include the now-larger 08-10) so its ratio drops to 2.14× and no longer qualifies.
  - Body text auto-populates from SQL output — no hardcoded numbers to update.

- **`opus-verbose-output`** (Rule 6): **PASS — unchanged**. Filter `%opus%`; SQL has no cost calc, only counts turns. TS saving math hardcodes `0.5 × output × $25/M` (Opus output rate). Opus 4.x/5 output rate unchanged at $25/M in new formula. Dry-run: `turns=12` (threshold 10, FIRES), `total_output=520,904 tokens, avg_ratio=19.11×`, potential saving `$6.51`. Would understate saving only if opus-4-1 (now $75/M) rows appeared — none in DB.

- **`opus-on-research-tasks`** (Rule 14): **PASS — unchanged**. Filter `%opus%`; uses `COST_SQL` for opus_cost. Opus 4.x/5 rates unchanged. Dry-run: `session_count=0` (below threshold=2) → correctly silent. Body text "5× lower cost" on Sonnet is a pre-existing overstatement (opus/sonnet-4-6 = 5:3 = 1.67× ratio; opus/sonnet-5 = 5:2 = 2.5× ratio; neither is 5×) — flagged below under spec hygiene. Not caused by pricing-update-002.

- **`opus-small-output`** (Rule 15): **PASS — unchanged**. Same as Rule 14: opus-only filter, opus rates unchanged, uses `opusCost × 0.80` for saving. Dry-run: `turns=0` → correctly silent. Same "5× lower" overstatement in body — pre-existing, not caused by pricing update.

- **Weekly digest `week_cost`**: **CHANGED — trivially**. Uses `COST_SQL`. Dry-run: `week_cost = $3,268.26 (old) → $3,267.56 (new)`. The fable-5 increase and sonnet-5 decrease nearly cancel in the last 7 days. No consumer of this number has a threshold, so no user-visible impact this week — but future weeks with different model mix will diverge more.

### Rules cost-adjacent but not driven by `COST_SQL` (saving math uses hardcoded Sonnet default rate)

These rules have model-agnostic queries (`WHERE model` is absent). The fire/silent condition does NOT change under new pricing. Only the TS-side `saving` figure uses hardcoded `$3/$0.30` (old Sonnet ELSE rates) which are still exact for sonnet-4-6 but stale for sonnet-5 and fable-5. All three are currently below their firing thresholds, so the stale saving math is not user-visible today.

- **`subagent-cache-miss`** (Rule 2): saving = `uncached × 0.7 × ($3 − $0.30) / M`. Same as old; not firing (cache ratio was 94.8% ≥ 30% threshold in May, remains healthy). If it fires later on sonnet-5 traffic it would **overstate** saving (~50% high, real premium is $1.80/M not $2.70/M); on fable traffic it would **understate** (~70% low, real premium is $9/M).
- **`cache-write-without-read`** (Rule 8): saving = `wasted_writes × $3 / M`. Same tension.
- **`prompt-caching-not-enabled`** (Rule 10): saving = `freshInput × 0.7 × ($3 − $0.30) / M`. Same tension.

**Recommendation**: Not urgent (rules aren't firing on this DB right now), but when the engineer touches `insights/route.ts` for `pricing-update-002`, they should replace the hardcoded `3.0` / `0.30` in these three saving formulas with per-row `COST_SQL` deltas (or explicitly document Sonnet-4-6 as the assumed model in `savingSubtext`). Filed for follow-up — do not block the pricing rollout on this.

### Rules unchanged (no cost dependency)

Not re-run — no `${COST_SQL}` reference in the query and no hardcoded dollar rate in body text or saving math:

- `edit-retries` (Rule 3)
- `long-running-tools` (Rule 4)
- `file-read-thrashing` (Rule 7)
- `tool-error-retry-loops` (Rule 9)
- `subagent-explosion` (Rule 11)
- `daily-volume-anomaly` (Rule 12)
- `high-tool-error-rate` (Rule 13)

### Spec doc hygiene

Flagged for future spec update — not fixing per audit-brief scope:

1. **`opus-trivial-tools.md`** line 13: "Cost calc uses Opus rates ($5/$25/$10/$0.50 per M)". Correct for opus-4.x/opus-5 (the opus family in DB) but silent on opus-4-1 which is now $15/$75. Not misleading given DB contents; add a one-line note about opus-4-1 for completeness next time this spec is touched.

2. **`opus-verbose-output.md`** lines 16, 22: "$25/M" hardcoded as Opus output rate. Correct for opus-4.x/5 (DB contents). Would understate saving on opus-4-1 (retired, 0 rows). No fix needed today.

3. **`opus-on-research-tasks.md`** body text (route.ts line 807, spec implicitly): "Sonnet handles them at 5× lower cost." Pre-existing overstatement — opus/sonnet ratio is 5:3 (sonnet-4-6, 1.67×) or 5:2 (sonnet-5, 2.5×), never 5:1. Under new pricing, sonnet-5 makes this claim less wrong but still not accurate. Pre-existing bug independent of pricing-update-002.

4. **`opus-small-output.md`** line 13: `savings ≈ verbose_turns × avg_cost × 0.8 (Sonnet is 1/5 the rate)`. Same pre-existing overstatement — Sonnet is 1/1.67 (sonnet-4-6) or 1/2.5 (sonnet-5), not 1/5. Pre-existing bug.

5. **`cache-write-without-read.md`** line 15: "paid the +25% premium for nothing". Under Sonnet-default the cache-write premium is $3/$3 = **+100%** of input rate, not +25%. Pre-existing spec-copy bug independent of pricing-update-002.

6. **`daily-cost-spike.md`** lines 17-19: May-13 dry-run numbers are old-audit historical. Not stale in a wrong-answer sense — just dated. Left as-is (audit brief said "flag only, don't fix").

### Summary for pricing-engineer

- **No rule needs to be silenced or re-designed** because of `pricing-update-002`.
- **One rule (Rule 5, daily-cost-spike) surfaces a different day** under the new formula. That is the correct behavior — 08-10 really was a fable-heavy day and its true cost is now visible.
- **Three cache-related rules (2/8/10) carry stale hardcoded Sonnet rates in their saving math**. Filed for follow-up in the same PR that updates `COST_SQL`; not user-visible on today's DB.
- **Two opus-vs-sonnet comparison rules (14/15) claim "5× lower on Sonnet"** in body text. Pre-existing overstatement, made slightly less wrong by sonnet-5's cheaper rate. Independent of `pricing-update-002` and out of audit scope; flag for PM to reword next copy pass.

*Dry-run completed 2026-08-19. DB: `~/.claude-dashboard/dashboard.db`. All queries substituted the new 7-branch COST_SQL directly into rule SQL; route.ts unchanged.*
